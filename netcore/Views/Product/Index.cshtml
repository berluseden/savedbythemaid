@model IEnumerable<JempSoft.Applications.Invent.Dto.ProductOutputDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLTE4Index.cshtml";
    ViewData["EntityIcon"] = "fa fa-tags";
    ViewData["ListTitle"] = "Product List";
    ViewData["IndexInfo"] = "Product is a merchandise that can be offered to the market for sell to satisfy demand. Before you can sell products to customers, you should have the product stock in hand by buying the product from vendors.";
}

@if (Model == null || !Model.Any())
{
    <div class="alert alert-info">
        <h4><i class="icon fa fa-info"></i> No Products Found</h4>
        There are no products registered yet. <a asp-action="Create" class="alert-link">Create your first product</a> to get started.
    </div>
}
else
{
<table class="table table-hover table-striped" style="width:100%">
    <thead>
        <tr>
            <th></th>
            <th>
                @Html.DisplayNameFor(model => model.ProductCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductName)
            </th>
            <th class="hidden">
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th class="hidden">
                @Html.DisplayNameFor(model => model.Barcode)
            </th>
            <th class="hidden">
                @Html.DisplayNameFor(model => model.SerialNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductType)
            </th>
            <th class="hidden">
                @Html.DisplayNameFor(model => model.Uom)
            </th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <div class="tools">
                        <a asp-action="Edit" asp-route-id="@item.ProductId" class="btn btn-primary btn-sm"><i class="fa fa-pencil"></i></a>
                        <a asp-action="Details" asp-route-id="@item.ProductId" class="btn btn-primary btn-sm"><i class="fa fa-list"></i></a>
                        <a asp-action="Delete" asp-route-id="@item.ProductId" class="btn btn-danger btn-sm"><i class="fa fa-trash"></i></a>
                    </div>
                    
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductName)
                </td>
                <td class="hidden">
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td class="hidden">
                    @Html.DisplayFor(modelItem => item.Barcode)
                </td>
                <td class="hidden">
                    @Html.DisplayFor(modelItem => item.SerialNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductType)
                </td>
                <td class="hidden">
                    @Html.DisplayFor(modelItem => item.Uom)
                </td>

            </tr>
        }
    </tbody>
</table>
}
