@model netcore.Models.CleaningPlaceRoom

@{
    ViewData["Title"] = "Editar Tipo de Habitación";
    Layout = "~/Views/Shared/_AdminLTE4Edit.cshtml";
    ViewData["EntityIcon"] = "fa fa-th-large";
    ViewData["FormTitle"] = "Editar Tipo de Habitación";
}

@if (ViewBag.ErrorMessage != null)
{
    <div class="alert alert-danger alert-dismissible">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <h4><i class="icon fa fa-ban"></i> Error</h4>
        @ViewBag.ErrorMessage
    </div>
}

<div class="row">
    <div class="col-md-12">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="CleaningPlaceRoomId" />
        <input type="hidden" asp-for="CreatorUserId" />
        <input type="hidden" asp-for="CreationDate" />
        
        <div class="form-group">
            <label asp-for="Title" class="control-label"><i class="fa fa-th-large text-muted"></i> Nombre de la Habitación <span class="text-danger">*</span></label>
            <input asp-for="Title" class="form-control" placeholder="Ej: Sala, Cocina" />
            <span asp-validation-for="Title" class="text-danger"></span>
        </div>

        <div class="form-group">
            <div class="checkbox">
                <label style="font-weight: 600;">
                    <input asp-for="IsActive" /> <i class="fa fa-check-circle text-success"></i> Activo
                </label>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

